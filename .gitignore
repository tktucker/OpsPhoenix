# -----------------------------------------------
# 1. TERRAFORM FILES (REQUIRED)
# -----------------------------------------------

# Terraform state files (contain state and often secrets/sensitive values)
*.tfstate
*.tfstate.*
# Terraform lock file, which records provider versions
.terraform.lock.hcl

# Local directories created by Terraform/CLI
.terraform/
.terraformrc
.tflint.hcl

# Crash log files from Terraform runs
crash.log

# Key files/variables used for temporary local testing (often contain secrets)
*.tfvars
!terraform.tfvars  # KEEP if you use a generic, committed tfvars file
*.tfvars.json

# Backup files (often created when using 'terraform apply' without -auto-approve)
# but should NOT be used with proper state management (e.g., S3 backend)
*.backup

# Provider dependencies and temporary files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# -----------------------------------------------
# 2. AWS CREDENTIALS & SENSITIVE DATA
# -----------------------------------------------

# AWS CLI credentials file (contains Access Keys)
# This is typically in your home directory, but good to include if copied locally.
.aws/credentials

# AWS CLI config file
.aws/config

# Credentials files for tools that integrate with AWS
aws.yml
secrets.json

# -----------------------------------------------
# 3. GENERAL DEVELOPMENT / OS FILES
# -----------------------------------------------

# IDE (VS Code) files
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/extensions.json
!.vscode/*.code-snippets

# Operating System files (macOS, Windows, Linux)
.DS_Store
Thumbs.db
ehthumbs.db
